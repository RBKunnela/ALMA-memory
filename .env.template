# =============================================================================
# ALMA Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# =============================================================================
# EMBEDDING PROVIDER CONFIGURATION
# =============================================================================
# ALMA supports three embedding providers: local, azure, and mock.
# Set embedding_provider in your .alma/config.yaml to choose which one to use.

# -----------------------------------------------------------------------------
# Option 1: Local Embeddings (Default - Recommended for Development)
# -----------------------------------------------------------------------------
# No environment variables needed!
# Uses sentence-transformers with all-MiniLM-L6-v2 model (384 dimensions).
# Install with: pip install sentence-transformers
#
# Pros: Free, offline, fast (~10ms), no API keys
# Cons: Lower accuracy than cloud models
#
# To use a different local model, modify the code:
#   LocalEmbedder(model_name="all-mpnet-base-v2")  # 768 dims, better quality

# -----------------------------------------------------------------------------
# Option 2: Azure OpenAI Embeddings (Recommended for Production)
# -----------------------------------------------------------------------------
# Uncomment and fill in these values if using embedding_provider: azure

# Your Azure OpenAI resource endpoint
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# API key for Azure OpenAI (or use Key Vault reference in config.yaml)
# AZURE_OPENAI_KEY=your-api-key-here

# Deployment name for your embedding model
# Common deployments: text-embedding-3-small (1536 dims), text-embedding-3-large (3072 dims)
# AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-small

# -----------------------------------------------------------------------------
# Option 3: Mock Embeddings (Testing Only)
# -----------------------------------------------------------------------------
# No environment variables needed.
# Generates deterministic hash-based embeddings for unit tests.
# Set embedding_provider: mock in config.yaml

# =============================================================================
# STORAGE BACKEND CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL with pgvector (Recommended for Production)
# -----------------------------------------------------------------------------
# Only needed if using storage: postgres in config.yaml
# Install with: pip install 'alma-memory[postgres]'
# Prerequisites: PostgreSQL with pgvector extension installed

# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DATABASE=alma_memory
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your-password-here

# For Azure PostgreSQL Flexible Server:
# POSTGRES_HOST=psql-your-server.postgres.database.azure.com
# POSTGRES_USER=adminuser
# POSTGRES_PASSWORD=your-password-here
# SSL is required: ssl_mode: require in config.yaml

# -----------------------------------------------------------------------------
# Azure Cosmos DB (Alternative Production Storage)
# -----------------------------------------------------------------------------
# Only needed if using storage: azure in config.yaml

# AZURE_COSMOS_ENDPOINT=https://your-cosmos-account.documents.azure.com:443/
# AZURE_COSMOS_KEY=your-cosmos-key-here

# Or use Azure Key Vault for secrets (recommended for production):
# In config.yaml: cosmos_key: ${KEYVAULT:alma-cosmos-key}
# AZURE_KEYVAULT_URL=https://your-keyvault.vault.azure.net/

# =============================================================================
# PYPI CONFIGURATION (Maintainers Only)
# =============================================================================
# For manual package uploads. GitHub Actions uses Trusted Publishing instead.

# PYPI_USERNAME=__token__
# PYPI_TOKEN=pypi-your-token-here

# =============================================================================
# DIMENSION REFERENCE
# =============================================================================
# Make sure embedding_dim in config.yaml matches your embedding model:
#
# | Model                    | Dimensions |
# |--------------------------|------------|
# | all-MiniLM-L6-v2         | 384        |
# | all-mpnet-base-v2        | 768        |
# | text-embedding-3-small   | 1536       |
# | text-embedding-3-large   | 3072       |
# | text-embedding-ada-002   | 1536       |
